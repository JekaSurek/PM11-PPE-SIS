<Window x:Class="УчетСИЗ.ViewModels.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:УчетСИЗ.ViewModels"
        mc:Ignorable="d"
        Title="Учёт СИЗ: Авторизация" 
        Height="350" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="DarkGray">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="10,5,10,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="10,0,10,5"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая часть с информацией -->
        <StackPanel Grid.Column="0" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center">
            <Image Source="/Images/Icon.jpg" 
                   Width="150" 
                   Height="150" 
                   Margin="0,0,0,20"/>
            <TextBlock Text="Система учёта" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="средств индивидуальной" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="защиты сотрудников" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Column="1" Background="GhostWhite" Margin="10" 
                CornerRadius="10">
            <StackPanel VerticalAlignment="Center"  HorizontalAlignment="Center" Width="250">
                <TextBlock Text="Авторизация" FontSize="20"  FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="Black"/>

                <TextBlock Text="Логин:" Foreground="Black"/>
                <TextBox x:Name="LoginTextBox"/>
                <TextBlock Text="Пароль:" Foreground="Black"/>
                <TextBox x:Name="PasswordTextBox"/>
                <Button Style="{StaticResource AnimatedOvalButton}" HorizontalAlignment="Stretch" VerticalAlignment="Center" x:Name="ButtonLogin" Content="Войти" Click="ButtonLogin_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
